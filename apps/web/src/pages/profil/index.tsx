import { ProfilMobile } from './ProfilMobile';

/**
 * Profil — on réutilise la vue « mobile » (carte héro premium) aussi sur la
 * version web/desktop, en pleine largeur de l'écran.
 */
export function ProfilPage() {
  return <ProfilMobile />;
}
